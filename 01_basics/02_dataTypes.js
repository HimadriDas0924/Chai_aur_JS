"use strict" // treat all code as newer version of JS, even if we don't mention it, entire code is treated as in strict mode

// alert(3+3) // we're cur on nodejs not browser, so it has different syntax 

// primitive data type:

// number, bigint, string, boolean, symbol
// null => no value (standalone value)
// undefined => value has not been assigned

// object:

console.log(typeof("hitesh")) // string 
console.log(typeof(null)) // object
console.log(typeof undefined); // undefined